```sql
SELECT * FROM FifaDB;
```

```sql
DELETE FROM FifaDB WHERE id=192;
```

```sql
INSERT INTO FifaDB VALUES (
            TestCountry1, 
            TestConfederation2, 
            0.1, 
            0.2, 
            0.3);
```

```sql
SELECT * FROM FifaDB;
```

```sql
UPDATE FifaDB SET 
        country=TestCountry1, 
        confederation=TestConfederation2,
        population_share=1.1, 
        tv_audience_share=1.2, 
        gdp_weighted_share=1.3 
        WHERE id=192;
```

```sql
DELETE FROM FifaDB WHERE id=192;
```

```sql
SELECT * FROM FifaDB;
```

```sql
INSERT INTO FifaDB VALUES (
            TestCountry1, 
            TestConfederation2, 
            0.1, 
            0.2, 
            0.3);
```

```sql
UPDATE FifaDB SET 
        country=TestCountry1, 
        confederation=TestConfederation2,
        population_share=1.1, 
        tv_audience_share=1.2, 
        gdp_weighted_share=1.3 
        WHERE id=192;
```

```sql
DELETE FROM FifaDB WHERE id=192;
```

```sql
SELECT * FROM FifaDB;
```

```sql
INSERT INTO FifaDB VALUES (
            TestCountry1, 
            TestConfederation2, 
            0.1, 
            0.2, 
            0.3);
```

```sql
UPDATE FifaDB SET 
        country=TestCountry1, 
        confederation=TestConfederation2,
        population_share=1.1, 
        tv_audience_share=1.2, 
        gdp_weighted_share=1.3 
        WHERE id=192;
```

```sql
DELETE FROM FifaDB WHERE id=192;
```

```sql
SELECT * FROM FifaDB;
```

```sql
INSERT INTO FifaDB VALUES (
            TestCountry1, 
            TestConfederation2, 
            0.1, 
            0.2, 
            0.3);
```

```sql
UPDATE FifaDB SET 
        country=TestCountry1, 
        confederation=TestConfederation2,
        population_share=1.1, 
        tv_audience_share=1.2, 
        gdp_weighted_share=1.3 
        WHERE id=192;
```

```sql
DELETE FROM FifaDB WHERE id=192;
```

```sql
SELECT * FROM FifaDB;
```

```sql
INSERT INTO FifaDB VALUES (
            TestCountry1, 
            TestConfederation2, 
            0.1, 
            0.2, 
            0.3);
```

```sql
UPDATE FifaDB SET 
        country=TestCountry1, 
        confederation=TestConfederation2,
        population_share=1.1, 
        tv_audience_share=1.2, 
        gdp_weighted_share=1.3 
        WHERE id=192;
```

```sql
DELETE FROM FifaDB WHERE id=192;
```

```sql
SELECT * FROM FifaDB;
```

```sql
INSERT INTO FifaDB VALUES (
            TestCountry1, 
            TestConfederation2, 
            0.1, 
            0.2, 
            0.3);
```

```sql
UPDATE FifaDB SET 
        country=TestCountry1, 
        confederation=TestConfederation2,
        population_share=1.1, 
        tv_audience_share=1.2, 
        gdp_weighted_share=1.3 
        WHERE id=192;
```

```sql
DELETE FROM FifaDB WHERE id=192;
```

```sql
SELECT * FROM FifaDB;
```

```sql
INSERT INTO FifaDB VALUES (
            TestCountry1, 
            TestConfederation2, 
            0.1, 
            0.2, 
            0.3);
```

```sql
UPDATE FifaDB SET 
        country=TestCountry1, 
        confederation=TestConfederation2,
        population_share=1.1, 
        tv_audience_share=1.2, 
        gdp_weighted_share=1.3 
        WHERE id=192;
```

```sql
DELETE FROM FifaDB WHERE id=192;
```

```sql
SELECT * FROM FifaDB;
```

```sql
INSERT INTO FifaDB VALUES (
            TestCountry1, 
            TestConfederation2, 
            0.1, 
            0.2, 
            0.3);
```

```sql
UPDATE FifaDB SET 
        country=TestCountry1, 
        confederation=TestConfederation2,
        population_share=1.1, 
        tv_audience_share=1.2, 
        gdp_weighted_share=1.3 
        WHERE id=192;
```

```sql
DELETE FROM FifaDB WHERE id=192;
```

```sql
SELECT * FROM FifaDB;
```

```sql
INSERT INTO FifaDB VALUES (
            TestCountry1, 
            TestConfederation2, 
            0.1, 
            0.2, 
            0.3);
```

```sql
UPDATE FifaDB SET 
        country=TestCountry1, 
        confederation=TestConfederation2,
        population_share=1.1, 
        tv_audience_share=1.2, 
        gdp_weighted_share=1.3 
        WHERE id=192;
```

```sql
DELETE FROM FifaDB WHERE id=192;
```

```sql
SELECT * FROM FifaDB;
```

```sql
INSERT INTO FifaDB VALUES (
            TestCountry1, 
            TestConfederation2, 
            0.1, 
            0.2, 
            0.3);
```

```sql
UPDATE FifaDB SET 
        country=TestCountry1, 
        confederation=TestConfederation2,
        population_share=1.1, 
        tv_audience_share=1.2, 
        gdp_weighted_share=1.3 
        WHERE id=192;
```

```sql
DELETE FROM FifaDB WHERE id=192;
```

```sql
SELECT * FROM FifaDB;
```

```sql
INSERT INTO FifaDB VALUES (
            TestCountry1, 
            TestConfederation2, 
            0.1, 
            0.2, 
            0.3);
```

```sql
UPDATE FifaDB SET 
        country=TestCountry1, 
        confederation=TestConfederation2,
        population_share=1.1, 
        tv_audience_share=1.2, 
        gdp_weighted_share=1.3 
        WHERE id=192;
```

```sql
DELETE FROM FifaDB WHERE id=192;
```

```sql
SELECT * FROM FifaDB;
```

```sql
INSERT INTO FifaDB VALUES (
            TestCountry1, 
            TestConfederation2, 
            0.1, 
            0.2, 
            0.3);
```

```sql
UPDATE FifaDB SET 
        country=TestCountry1, 
        confederation=TestConfederation2,
        population_share=1.1, 
        tv_audience_share=1.2, 
        gdp_weighted_share=1.3 
        WHERE id=192;
```

```sql
DELETE FROM FifaDB WHERE id=192;
```

```sql
SELECT * FROM FifaDB;
```

```sql
INSERT INTO FifaDB VALUES (
            TestCountry1, 
            TestConfederation2, 
            0.1, 
            0.2, 
            0.3);
```

```sql
UPDATE FifaDB SET 
        country=TestCountry1, 
        confederation=TestConfederation2,
        population_share=1.1, 
        tv_audience_share=1.2, 
        gdp_weighted_share=1.3 
        WHERE id=192;
```

```sql
DELETE FROM FifaDB WHERE id=192;
```

```sql
SELECT * FROM FifaDB;
```

```sql
INSERT INTO FifaDB VALUES (
            TestCountry1, 
            TestConfederation2, 
            0.1, 
            0.2, 
            0.3);
```

```sql
UPDATE FifaDB SET 
        country=TestCountry1, 
        confederation=TestConfederation2,
        population_share=1.1, 
        tv_audience_share=1.2, 
        gdp_weighted_share=1.3 
        WHERE id=192;
```

```sql
DELETE FROM FifaDB WHERE id=192;
```

```sql
SELECT * FROM FifaDB;
```

```sql
INSERT INTO FifaDB VALUES (
            TestCountry1, 
            TestConfederation2, 
            0.1, 
            0.2, 
            0.3);
```

```sql
UPDATE FifaDB SET 
        country=TestCountry1, 
        confederation=TestConfederation2,
        population_share=1.1, 
        tv_audience_share=1.2, 
        gdp_weighted_share=1.3 
        WHERE id=192;
```

```sql
DELETE FROM FifaDB WHERE id=192;
```

```sql
SELECT * FROM FifaDB;
```

```sql
INSERT INTO FifaDB VALUES (
            TestCountry1, 
            TestConfederation2, 
            0.1, 
            0.2, 
            0.3);
```

```sql
UPDATE FifaDB SET 
        country=TestCountry1, 
        confederation=TestConfederation2,
        population_share=1.1, 
        tv_audience_share=1.2, 
        gdp_weighted_share=1.3 
        WHERE id=192;
```

```sql
DELETE FROM FifaDB WHERE id=192;
```

```sql
INSERT INTO FifaDB VALUES (
            TestCountry1, 
            TestConfederation2, 
            0.1, 
            0.2, 
            0.3);
```

```sql
SELECT * FROM FifaDB;
```

```sql
UPDATE FifaDB SET 
        country=TestCountry1, 
        confederation=TestConfederation2,
        population_share=1.1, 
        tv_audience_share=1.2, 
        gdp_weighted_share=1.3 
        WHERE id=192;
```

```sql
DELETE FROM FifaDB WHERE id=192;
```

```sql
SELECT * FROM FifaDB;
```

```sql
INSERT INTO FifaDB VALUES (
            TestCountry1, 
            TestConfederation2, 
            0.1, 
            0.2, 
            0.3);
```

```sql
UPDATE FifaDB SET 
        country=TestCountry1, 
        confederation=TestConfederation2,
        population_share=1.1, 
        tv_audience_share=1.2, 
        gdp_weighted_share=1.3 
        WHERE id=192;
```

```sql
DELETE FROM FifaDB WHERE id=192;
```

